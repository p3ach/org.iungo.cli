package org.iungo.cli.api;


/**
 * 
 * We need a defined Class so the RootNode can help create the TDBNode.
 * 
 * @author Dick
 *
 */
public class CreateTDBNodeWord extends AbstractCreateNodeWord {

	public CreateTDBNodeWord(final NodeContextWord nodeContextWord) {
		super(nodeContextWord);
	}
}
